<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="TraePrimary" Color="#2D2D2D" />
    <SolidColorBrush x:Key="TraePrimaryHover" Color="#202020" />
    <SolidColorBrush x:Key="TraeSecondary" Color="#2D2D2D" />
    <SolidColorBrush x:Key="TraeAccent" Color="#2D2D2D" />
    <SolidColorBrush x:Key="TraeError" Color="#2D2D2D" />
    <SolidColorBrush x:Key="TraeSuccess" Color="#2D2D2D" />
    <SolidColorBrush x:Key="TraeWarning" Color="#2D2D2D" />

    <SolidColorBrush x:Key="TraeBackground" Color="#181818" />
    <SolidColorBrush x:Key="TraeActivityBar" Color="#1B1B1B" />
    <SolidColorBrush x:Key="TraeSideBar" Color="#1B1B1B" />
    <SolidColorBrush x:Key="TraeSurface" Color="#202020" />
    <SolidColorBrush x:Key="TraeSurfaceLight" Color="#2D2D2D" />
    <SolidColorBrush x:Key="TraeEditor" Color="#181818" />
    <SolidColorBrush x:Key="TraeEditorAlt" Color="#202020" />
    <SolidColorBrush x:Key="TraeBorder" Color="#2D2D2D" />
    <SolidColorBrush x:Key="TraeTextPrimary" Color="#B8B8B8" />
    <SolidColorBrush x:Key="TraeTextSecondary" Color="#9A9A9A" />
    <SolidColorBrush x:Key="TraeTextDisabled" Color="#6F6F6F" />

    <Style TargetType="GroupBox">
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Background" Value="{StaticResource TraeSurface}" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top"
                                       Text="{TemplateBinding Header}"
                                       Foreground="{StaticResource TraeTextSecondary}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8" />
                            <ContentPresenter />
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Margin" Value="3" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource TraePrimary}" />
        <Setter Property="Background" Value="{StaticResource TraePrimary}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#202020" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#2D2D2D" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#181818" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#202020" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#202020" />
                            <Setter Property="Foreground" Value="{StaticResource TraeTextDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Background" Value="{StaticResource TraePrimaryHover}" />
                <Setter Property="BorderBrush" Value="{StaticResource TraePrimaryHover}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="true">
                <Setter Property="Opacity" Value="0.85" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" Value="#202020" />
                <Setter Property="BorderBrush" Value="#202020" />
                <Setter Property="Foreground" Value="{StaticResource TraeTextDisabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="Background" Value="{StaticResource TraeEditor}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="CaretBrush" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="SelectionBrush" Value="#2D2D2D" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property="BorderBrush" Value="{StaticResource TraePrimary}" />
            </Trigger>
            <Trigger Property="IsReadOnly" Value="true">
                <Setter Property="Background" Value="{StaticResource TraeEditorAlt}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Background" Value="{StaticResource TraeEditor}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Background="#181818"
                                      BorderBrush="#2D2D2D"
                                      BorderThickness="1">
                            <Grid>
                                <ContentPresenter Margin="6,2,22,2"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" />
                                <Path HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"
                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                      Fill="#9A9A9A" />
                            </Grid>
                        </ToggleButton>
                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="True"
                               Focusable="False"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               PopupAnimation="None">
                            <Border Background="#181818"
                                    BorderBrush="#2D2D2D"
                                    BorderThickness="1"
                                    MinWidth="{TemplateBinding ActualWidth}">
                                <ScrollViewer Background="#181818"
                                              BorderThickness="0"
                                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="ToggleButton" Property="Background" Value="#202020" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{StaticResource TraeTextDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="#181818" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="true">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#2D2D2D" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#202020" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Margin" Value="2,1" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextSecondary}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Margin" Value="4" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Margin" Value="4" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
    </Style>

    <Style TargetType="TreeView">
        <Setter Property="Background" Value="{StaticResource TraeSideBar}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="4" />
    </Style>

    <Style TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Padding" Value="4,3" />
        <Setter Property="Cursor" Value="Hand" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="true">
                <Setter Property="Background" Value="#2D2D2D" />
                <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Background" Value="#202020" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource TraeSurface}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{StaticResource TraeSurface}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextDisabled}" />
        <Setter Property="Padding" Value="14,7" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="TabBorder" Property="Background" Value="#181818" />
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="#2D2D2D" />
                            <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="TabBorder" Property="Background" Value="#202020" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="true">
                <Setter Property="Background" Value="{StaticResource TraeBackground}" />
                <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="BorderBrush" Value="{StaticResource TraePrimary}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Background" Value="#202020" />
                <Setter Property="Foreground" Value="{StaticResource TraeTextSecondary}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource TraeSurfaceLight}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{StaticResource TraeSurfaceLight}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextSecondary}" />
        <Setter Property="Padding" Value="9,5" />
        <Setter Property="FontSize" Value="12" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Background" Value="#2D2D2D" />
                <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="Thumb">
        <Setter Property="Background" Value="#2D2D2D" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="#181818" />
        <Setter Property="Foreground" Value="#2D2D2D" />
        <Setter Property="Width" Value="10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid x:Name="Root" Background="#181818">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Background="#2D2D2D" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Width" Value="Auto" />
                            <Setter Property="Height" Value="10" />
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="#202020" />
        <Setter Property="Foreground" Value="{StaticResource TraePrimary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="8" />
    </Style>

    <Style TargetType="RichTextBox">
        <Setter Property="Background" Value="{StaticResource TraeEditor}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="CaretBrush" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="SelectionBrush" Value="#2D2D2D" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RichTextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="#202020" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="StatusBarItem">
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="Padding" Value="8,0" />
    </Style>

    <Style TargetType="GridSplitter">
        <Setter Property="Background" Value="{StaticResource TraeBackground}" />
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource TraeSurfaceLight}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource TraeEditor}" />
        <Setter Property="Foreground" Value="{StaticResource TraeTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource TraeBorder}" />
    </Style>

</ResourceDictionary>
